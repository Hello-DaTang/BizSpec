---
name: bizspec
description: 将会议纪要、Excel、真实业务操作和最终成果逐步还原为有状态、可确认、可进入开发的业务规格。适用于业务部门只能描述工作流程、无法描述系统形态，且开发人员不熟悉业务领域的内部系统项目。
---

# BizSpec

## 目标

把模糊业务资料转换为一套可追踪的业务规格工作区。不要一开始设计页面、接口或数据库；先证明已经理解业务事实、数据来源、计算规则、异常处理、责任归属和业务对象状态。

最终目标不是生成一份漂亮文档，而是回答：

1. 当前业务实际上如何完成？
2. 哪些内容是事实，哪些只是推测？
3. 哪些规则已确认，哪些仍由业务决定？
4. 系统第一阶段要替代哪个最小业务闭环？
5. 当前是否已经具备进入技术设计或开发的条件？

## 何时使用

在以下情况使用本 Skill：

- 用户只有会议纪要、音转文、Excel 或零散说明；
- 业务人员只会讲“现在怎么做”，无法提出系统页面；
- 开发人员不了解业务，需要边调研边形成规格；
- 项目在零原型、零蓝图状态下启动；
- 需要分节点推进，并让每个节点具有状态和完成条件；
- 需要在开发前明确待确认规则、责任人和阻塞项。

若需求、验收标准和技术方案已经明确，应转入技术规格或编码流程，而不是继续扩大 BizSpec。

## 不可违反的规则

1. **所有节点必须有 `id`、`title`、`status`。**
2. **所有规则、问题、决策、异常和业务对象也必须有 `id` 与 `title`。**
3. 不得把 AI 推导、开发人员猜测或常见行业做法写成已确认事实。
4. 会议纪要中的陈述默认是 `draft` 或 `pending_confirmation`；只有明确记录为最终决议并可追溯时，才可标记 `confirmed`。
5. 没有真实样表时可以提出候选字段，但不得完成字段映射节点。
6. BOM、折料、核算口径、单位转换、业务状态定义和责任归属必须有业务 Owner。
7. IT 可以提出候选方案，但必须标记为 `inference` 或 `proposal`。
8. 节点存在未解决的关键阻塞时，不得标记为 `done`。
9. 不要为了显得完整而制造页面、字段、接口、审批步骤或系统集成方式。
10. 每次状态变化必须记录原因。

## 两层状态模型

### 节点状态

仅允许：

- `not_started`：尚未开始；
- `in_progress`：正在整理、访谈或补充；
- `blocked`：因资料、责任人、权限或业务结论缺失而无法继续；
- `review_required`：已有草稿，等待业务复核；
- `done`：通过节点完成校验；
- `waived`：明确不适用，并记录批准人和原因。

推荐流转：

```text
not_started → in_progress → review_required → done
                    └──────→ blocked

done → in_progress 仅用于规则变化或重新打开
```

### 条目状态

规则、字段、问题、决策和其他节点内部条目使用：

- `draft`
- `pending_confirmation`
- `confirmed`
- `rejected`
- `deprecated`

状态不能替代证据类型。每个条目还必须标记来源类型，例如：

- `source_statement`
- `confirmed_decision`
- `inference`
- `proposal`
- `open_question`

## 标准工作流

按依赖推进 12 个节点，不要强制一次完成全部节点。

1. `BS-01 项目范围与业务目标`
2. `BS-02 业务资料与真实案例`
3. `BS-03 角色与业务责任`
4. `BS-04 当前业务流程还原`
5. `BS-05 数据源与字段映射`
6. `BS-06 业务术语与数据字典`
7. `BS-07 业务规则与计算规则`
8. `BS-08 异常场景与处理方式`
9. `BS-09 目标业务流程`
10. `BS-10 业务对象状态模型`
11. `BS-11 第一阶段范围与验收场景`
12. `BS-12 开发就绪检查`

完整节点说明和完成条件见 `references/workflow.md`。

## 每次处理一个节点时

### 1. 读取当前状态

读取：

- `manifest.yaml`；
- 当前节点文件；
- 当前节点依赖；
- 与该节点相关的 sources 和 registers。

不要仅根据对话记忆更新项目。

### 2. 检查依赖

若关键依赖未完成：

- 允许整理现有证据和候选项；
- 不允许伪造依赖产物；
- 将无法继续的原因写入 `blockers`；
- 节点状态设置为 `blocked` 或保持 `in_progress`。

### 3. 分类所有内容

把输入分成：

- 已有证据支持的业务事实；
- 明确的会议决议；
- 待确认业务规则；
- AI 或 IT 推导；
- 缺失资料；
- 开放问题；
- 明确不做事项。

### 4. 更新节点产物

节点文件至少包含：

- 节点目标；
- 当前结论；
- 已确认内容；
- 待确认内容；
- 推导与候选方案；
- 阻塞项；
- 产物；
- 完成条件检查；
- 状态变更记录。

### 5. 更新登记簿

跨节点信息不要只留在正文中：

- 业务规则写入 `registers/rules.yaml`；
- 待确认事项写入 `registers/questions.yaml`；
- 已作出的选择写入 `registers/decisions.yaml`。

### 6. 执行完成校验

只有以下条件均满足时，才能标记 `done`：

- 必填标题存在；
- 必填产物存在；
- 关键条目有 Owner；
- 无未解决的关键阻塞；
- 需要业务确认的内容已有确认记录；
- 完成条件逐项为真。

### 7. 给出下一步

每次只给出一个最优先的下一动作。优先选择：

- 能解除最多下游阻塞的资料；
- 能确认关键规则的业务负责人；
- 同一真实业务案例的一整套输入和输出；
- 可以形成最小业务闭环的节点。

## 处理会议纪要

会议纪要是证据来源，不是完整需求规格。处理时必须：

1. 保存原始或校正后的纪要到 `sources/`；
2. 给来源分配唯一 ID；
3. 将内容分发到对应节点，而不是只生成一篇总结；
4. 为每条关键结论记录来源章节或原文位置；
5. 将歧义、同音误识别和冲突内容写入开放问题；
6. 不因纪要写了“系统应支持”就自动判定字段、交互和实现方式已经确认。

## 处理 Excel 和真实案例

优先收集同一个业务周期的一整套资料：

```text
原始输入 → 中间计算 → 人工修订 → 最终结果 → 对外报告
```

分析 Excel 时重点识别：

- 表头：业务字段和实体候选；
- 公式：计算规则候选；
- 颜色和批注：异常或人工判断；
- 多个版本文件：版本、审核、重算或发布需求；
- 复制粘贴：数据集成和字段映射需求；
- 手工汇总：可自动化的规则；
- “最终版/确认版”：状态流转和责任边界。

## 生成目标流程时

不要直接把现有流程电子化。先区分：

- 必须保留的业务控制；
- 因 Excel 或系统割裂产生的无效步骤；
- 可以自动化的转换和校验；
- 必须由人判断的业务例外。

目标流程必须形成一个最小业务闭环，而不是一组孤立页面。

## 生成业务对象状态时

状态模型与 Skill 节点状态完全分开。

业务对象状态必须：

- 对应真实生命周期；
- 明确进入和退出条件；
- 明确允许的操作、角色和回退方式；
- 避免把多个独立维度塞入一个巨大枚举；
- 对可并存问题优先使用主状态加校验标记。

## 输出风格

- 使用业务人员能确认的语言，不先使用技术术语包装未知内容；
- 结论、推导和问题分栏表达；
- 使用表格列出字段、规则、角色、异常和状态；
- 对不确定内容明确说明，不使用貌似确定的措辞；
- 不以页面原型代替业务分析；
- 不自动进入编码，除非 `BS-12` 已通过或用户明确接受风险。

## 支持的操作语义

用户可能使用命令式表达。按以下含义执行：

- `bizspec init`：初始化工作区和 12 个节点；
- `bizspec ingest`：导入来源，提取事实、问题、规则和推导；
- `bizspec status`：展示节点状态、阻塞项和开发就绪度；
- `bizspec next`：选择当前最值得推进的节点；
- `bizspec work BS-xx`：只处理指定节点及其直接登记簿；
- `bizspec review BS-xx`：生成业务确认清单，不自动确认；
- `bizspec complete BS-xx`：验证完成条件后再更新状态；
- `bizspec reopen BS-xx`：记录原因并重新打开；
- `bizspec export`：仅导出已确认、可追溯的开发输入。

命令可以由 `scripts/bizspec.py` 辅助执行，但 Agent 必须先遵守本 Skill 的证据和状态规则。